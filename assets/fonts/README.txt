Drop your custom font files here.

Expected (referenced in styles.css @font-face):
  labora-head.woff2   -> headline / condensed grotesque
  labora-mono.woff2   -> labels & body monospace

If your files have different names or formats (.otf/.ttf), tell me
the filenames and I will update the @font-face src paths.
.woff2 is preferred for the web (smallest, universally supported).
